# Đọc trước khi bắt tay: bảng trạng thái chung

Dự án này **không đứng một mình**. Nhiều dự án NERO cùng dùng một bộ Trang Facebook, một bộ sản phẩm
và một cơ sở dữ liệu. Trước khi chạm vào phần việc có thể thuộc dự án khác, đọc:

    /Users/ledinhquyet/Projects/Nero-mes/docs/TRANG_THAI_CHUNG.md

Ở đó ghi **ai đang làm gì** và **cái gì đang chờ Sếp**. Xem dòng nào đã cũ:

    npm run trangthai

**Ba luật của bảng đó:** chỉ ghi ai-đang-làm-gì và chờ-Sếp-cái-gì · số thật luôn đọc từ nguồn chứ
không bao giờ từ bảng · bảng nằm trong git để `git pull` báo xung đột.

**Không nhắn máy-tới-máy.** Các phiên Claude nhắn được cho nhau, nhưng Sếp đã quyết không dùng —
cần phối hợp gấp thì Sếp là người nối. Lý do: mọi thao tác chạm Meta bắt buộc phải qua Sếp duyệt
(`~/.claude/CLAUDE.md` mục 1), và luật đó viết cho người.
